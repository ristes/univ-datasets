WHERE {
	GRAPH ?gr {
		?g a univ:Grade.
		?c a univ:Curse.
	    ?g univ:for_course ?c.
       	?c univ:has_professor ?prof.
       	?c univ:subject ?subj.
       	?g ?p ?o .
       	FILTER ( ?prof =<pIRI> && ?subj=<sIRI> )
	}
	FILTER (?gr = <gIRI>)
}
